/* eslint-disable import/no-anonymous-default-export */
const BIN_WIDTH = `60px`
const HEADER_HEIGHT = "8rem"
const FOOTER_HEIGHT = "2rem"
const BIN_HEIGHT = `80px`
const BODY_HEIGHT = `20px`
const BODY_WIDTH_BASE = `40px`;

export default {
    BIN_WIDTH,
    HEADER_HEIGHT,
    FOOTER_HEIGHT,
    BIN_HEIGHT,
    BODY_HEIGHT,
    BODY_WIDTH_BASE
};